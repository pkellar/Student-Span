@model StudentSpan.ViewModels.StudentViewModel

@{ ViewData["Title"] = "Watch";
    Layout = "~/Views/Shared/_Layout.cshtml"; }

<h1>
    @Html.DisplayFor(model => model.LastName)
</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Student" asp-action="CreateWatch">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WatchText" class="control-label"></label>
                <input asp-for="WatchText" class="form-control" />
                <span asp-validation-for="WatchText" class="text-danger"></span>
            </div>
            <div class="form-group">

                <input type="hidden" asp-for="@Model.Id" />
                <input type="hidden" asp-for="@Model.FirstName" />
                <input type="hidden" asp-for="@Model.LastName" />
                <input type="submit" value="Add Watch" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}